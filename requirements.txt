fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# --- add when you wire up the real layers ---
# google-generativeai==0.8.3   # Gemini
# duckdb==1.1.1                 # dashboard analytics
# chromadb==0.5.5              # semantic claim<->rule matching
# sentence-transformers==3.1.1 # local embeddings for ChromaDB
